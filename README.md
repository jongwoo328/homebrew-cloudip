# homebrew-cloudip

Homebrew tap for [cloudip](https://github.com/jongwoo328/cloudip) - a CLI tool that identifies which cloud provider owns a given IP address.

## Installation

```shell
brew tap jongwoo328/cloudip
brew install cloudip
```

## Upgrade

```shell
brew upgrade cloudip
```
